<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "https://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow JIT (required by JVM / Compose) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (needed by JVM internals) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow DYLD environment variables used by the JVM launcher -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation so the JVM can load its own JARs -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Outbound network access (API / SSE / WebSocket) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Microphone – required for voice input / ASR -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Read/write files the user explicitly selects -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- Read/write the Downloads folder (attachment cache) -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
</dict>
</plist>
